These fixtures preserve the tarball link names from https://ftp.loongnix.cn/Java/
and its openjdk*/ directories, retrieved on 2026-09-12. Styling and unrelated
links were removed. They include both LoongArch ABIs, MIPS and early-access
packages so the tests can verify filtering without network access.
